#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0, i##_len = (n); i < i##_len; ++i)
#define xrep(i, a, b) for (int i = int(a); i < int(b); ++i)

string S;
vector<char> color;
int ans = 0;


int main() {
    int N;
    cin >> N >> S;

    queue<int> q;
    q.push(1)
    


    cout << ans << endl;
    return 0;
}